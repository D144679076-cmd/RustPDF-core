# License secret used to sign and verify license keys (HMAC-SHA256).
# Must be the same value at key-generation time and library compile time.
# Set this before running:
#   cargo build --features crypto --bin keygen
#   cargo build --features wasm --target wasm32-unknown-unknown
PDF_CORE_LICENSE_SECRET=your-secret-here-min-32-chars
